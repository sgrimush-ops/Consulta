# LEIAME

Este projeto executa a aplicacao principal em Streamlit e mantem sua documentacao operacional na pasta `doc/`.

Para iniciar localmente:

```bash
pip install -r requirements.txt
streamlit run main.py
```
